# ============================================
# CRONOS SNOW RAIL - BACKEND CONFIGURATION
# ============================================

# ============ WALLET & BLOCKCHAIN ============
# Your wallet private key (WITHOUT 0x prefix)
PRIVATE_KEY=your_private_key_here

# Cronos RPC endpoint
RPC_URL=https://evm-t3.cronos.org

# Chain ID (338 = Cronos Testnet)
CHAIN_ID=338

# Smart contract addresses
SETTLEMENT_CONTRACT_ADDRESS=0xae6E14caD8D4f43947401fce0E4717b8D17b4382
MIXER_CONTRACT_ADDRESS=0xfAef6b16831d961CBd52559742eC269835FF95FF

# ============ DATABASE ============
# PostgreSQL connection URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://cronos:cronos123@localhost:5432/cronos_snowrail

# ============ REDIS (Optional) ============
# Redis for WebSocket scaling and caching
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============ AUTHENTICATION ============
# Secret for JWT token signing
JWT_SECRET=change_this_to_a_random_secure_string_in_production

# ============ SERVER CONFIGURATION ============
# Server port
PORT=4000

# Server host
HOST=0.0.0.0

# Environment
NODE_ENV=development

# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ============ LOGGING ============
# Log level: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# ============ OPTIONAL FEATURES ============
# Use real price API (vs mock)
USE_REAL_PRICE_API=false

# Require wallet verification
REQUIRE_VERIFICATION=false

# Use ZK proofs
USE_ZK_PROOFS=false

# ZK provider: noir | mock
ZK_PROVIDER=mock

# Verification provider: cronos-verify | mock
VERIFY_PROVIDER=mock

# Cronos network name
CRONOS_NETWORK_NAME=Cronos Testnet
